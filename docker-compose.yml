version: "3.9"

services:
  app:
    image: node:20-alpine
    working_dir: /workspace
    command: sh -c "npm install && npm run dev -w @karaoke/frontend"
    volumes:
      - ./:/workspace
    ports:
      - "3000:3000"
    env_file:
      - .env
